## Unreleased

- Add `CheckoutHostContext`, `CheckoutHostProvider`, `useCheckoutHost` and
  `useOptionalCheckoutHost`: the single seam through which a host supplies navigation, close,
  notices, site id, URL params, event tracking and the completion callback.

## 1.0.0

- Initial version of `@automattic/checkout`.
